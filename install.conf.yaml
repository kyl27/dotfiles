- clean: ['~']

- link: 
    ~/.bashrc: bashrc
    ~/.vimrc: vimrc
    ~/.profile: profile
    ~/.gitconfig:
        path: gitconfig
        force: true
- shell: 
  - [git submodule update --init --recursive, Installing submodule]

