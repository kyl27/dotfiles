- clean: ['~']

- link: 
    ~/.bashrc: bashrc
    ~/.vimrc: vimrc
    ~/.tmux.conf: tmux.conf
    ~/.colordiffrc: colordiffrc
    ~/.gitconfig:
        path: gitconfig
        force: true
- shell: 
  - [git submodule update --init --recursive, Installing submodule]

